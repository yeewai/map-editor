'use strict';

var _actions = require('./actions');

var actions = _interopRequireWildcard(_actions);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

describe('Modal Actions', function () {
    it('should create an action to show modal', function () {
        var props = { a: 1 };
        var expectedAction = {
            type: 'modal/SHOW',
            modalType: 'a',
            modalProps: props
        };
        expect(actions.showModal('a', props)).toEqual(expectedAction);
    });

    it('should create an action to hide modal', function () {
        var expectedAction = {
            type: 'modal/HIDE'
        };
        expect(actions.hideModal()).toEqual(expectedAction);
    });
});