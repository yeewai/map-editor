'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactRedux = require('react-redux');

var _index = require('../index');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * How to display Modals
 * 	* Import new modal component
 * 	* Create a key:value mapping in modal_components
 * 	* Use the OpenModalButton component to display link to open
 */

var ModalRoot = function ModalRoot(_ref) {
    var modals = _ref.modals,
        modal_components = _ref.modal_components;

    var keys = modals ? Object.keys(modals) : [];
    var modalType = keys.find(function (k) {
        return k in modal_components && modals[k].visible;
    });

    if (!modalType) {
        return null;
    }

    var SpecificModal = modal_components[modalType];
    return _react2.default.createElement(SpecificModal, _extends({ modalType: modalType }, modals[modalType].modalProps));
};

var mapStateToProps = function mapStateToProps(state, ownProps) {
    return {
        modals: _index.modalSelectors.getAllModals(state)
    };
};
exports.default = (0, _reactRedux.connect)(mapStateToProps)(ModalRoot);